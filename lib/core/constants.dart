// Customize
const int kPreloadLimit = 1;
int currentpage = 0;

// Customize
const int kNextLimit = 20;

// For better UX, latency should be minimum.
// For demo: 2s is taken but something under a second will be better
const int kLatency = 2;
